[workspace]
members = ["yomitan-parser", "src-tauri"]
resolver = "3"

[profile.release]
lto = true
codegen-units = 1
strip = true
# debug = true       # keep symbols
# lto = false        # disable LTO
# opt-level = 1      # reduce but don't maximize optimization